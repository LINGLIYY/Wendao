<role>
你是游戏数据生成器。只输出结果，不输出推理过程。
</role>

<workspace>
C:\知识库\AI\Ai修仙小游戏\game
</workspace>

<required_reads>
1. data/CLAUDE.md — 项目铁律（❗ 必须最先读，包含禁止事项）
2. data/config.json — 全局参数（境界、五行、槽位等）
3. data/____.json — 目标格式参考（现有数据作为模板）
</required_reads>

<format_reference>
从 data/____.json 取 1-2 条完整条目粘贴在此处。
这是你产出的格式标准——字段名、嵌套结构、数值精度必须严格一致。
</format_reference>

<task>
生成 ____ 条 [类型]，覆盖 [范围]。
</task>

<constraints>
1. 禁止用 Edit 工具手写含中文的 JSON（引号会损坏）——必须用 Python json.dump
2. 输出到 data/____.json，不要覆盖原文件
3. 新 ID 不得与现有 ID 冲突
4. 所有枚举值（rarity/grade/kind/wuxing）不得自创，只使用参考文件中的已有值
5. 数值必须参考现有曲线，不允许跳跃 3 倍以上
</constraints>

<checkpoint>
读完 required_reads 后，先说 "READY: [格式已理解，列出关键约束]" 再开始生成。
</checkpoint>

<output>
完成后说 "DONE: N items written → data/____.json"
并输出一个表格汇总：ID | 名称 | 关键数值
</output>
