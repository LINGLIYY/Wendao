<role>你是 Python 代码恢复专家。工作目录 C:\知识库\AI\Ai修仙小游戏\game。</role>

<context>
state.py 的前 720 行（类声明 + __init__ + ~30 个方法）被误删了。
但文件剩余部分（to_dict/upgrade/save/load/__main__）完好，__pycache__/state.cpython-313.pyc 也完整。
</context>

<task>
重建 state.py。需要你：
1. 读取 data/CLAUDE.md 中 GameState 的接口描述
2. 读取 server.py 和 engine.py 中对 state 的所有 import 和调用（确定需要哪些方法）
3. 检查 __pycache__/state.cpython-313.pyc 的字节码（确定方法签名）
4. 读取当前 state.py 的尾部代码（to_dict/upgrade 之后的内容，仍然有效）
5. 拼出完整的 state.py

完成后用 python -m py_compile state.py 验证编译通过。
</task>

<constraints>
- 文件头必须有 # -*- coding: utf-8 -*- 和完整 docstring
- 导入: json, os, random, time, Optional from typing
- 类名: GameState，必须包含 CLAUDE.md 列出的所有必要方法
- 内部数据保存在 self._d ("data" dict) 和 self.config
- 属性键双命名空间（中文键在 JSON 数据中，ASCII 键在内部存储）
- 完成后说 DONE
</constraints>
